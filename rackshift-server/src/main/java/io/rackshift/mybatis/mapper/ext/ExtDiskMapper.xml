<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.rackshift.mybatis.mapper.ext.ExtDiskMapper">

    <select id="selectAllDisks"  resultType="io.rackshift.model.DiskDto">
    select bare_metal.machine_sn, disk.*
    from bare_metal
    left join disk
    on bare_metal.id = disk.bare_metal_id
    where disk.id is not null
  </select>
</mapper>